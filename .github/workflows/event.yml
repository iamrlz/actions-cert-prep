name: Push Events
on:
 issues:
   types:
     - opened 
jobs:
  show-info:
      runs-on: ubuntu-latest
      steps:
      - name: info
        run:
          echo "This is a push event on all the branches"
